# `_prose-budget` — shared evidence

Python and TypeScript share the same sentence-boundary protections, prose
bands, and exemption categories. Tests pin URLs, code spans, decimals,
abbreviations, contiguous line comments, generated files, story files,
directives, licenses, runtime documentation tags, and PR 4213 examples.

The first-party census and external-corpus rationale live in SARJ091 because
that blocking rule owns the threshold decision.
