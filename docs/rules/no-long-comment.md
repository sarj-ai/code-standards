# no-long-comment

Three sentences is the blocking threshold because prose at that size routinely mixes local mechanics with durable design context. Keep the local constraint in code and move rationale that must survive refactors to an ADR or focused documentation.

The paired corpus report measures the one-, two-, and three-plus-sentence bands independently so the error threshold cannot drift merely to increase finding counts.
