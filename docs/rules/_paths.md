# `_paths` — evidence

Shared helper. This file holds what the code cannot: the measurements behind
each threshold, the false-positive families the guards exist to stop, and the
alternatives that were rejected.

Shared file-path predicates for rules that are scoped by file
kind rather than by syntax — a timing-attack surface only exists in
production auth code, fixture SQL in a test legitimately breaks the
store-write conventions, and a dev script legitimately skips production
hardening. Answering "is this a test file?" / "is this a script?" in exactly
one place (mirroring the Python `_paths.py` extraction) is what stops the
per-rule regexes from drifting apart again.

## Evidence relocated from the source

### `*`

The `__…__` fixture spellings are recognised for the same reason as the
`-test.ts` suffix below: a fixture is INPUT to a test, and its exact text is
usually asserted. `storybook/code/renderers/react/src/componentManifest/
__testfixtures__/ForwardRef.tsx` documents three props with `/** Input label
*\/`-style JSDoc that `componentMetaExtractor.qa.test.ts:448` compares
string-for-string; a rule that told the author to delete those comments
would be telling them to break the test the file exists for.

The `-test.ts` / `_test.ts` suffixes are recognised because the dot form
alone is not the universal convention, and missing them silently un-exempts a
whole repo's test tree. Measured while converging `no-repeated-string-literal`
with its Python twin (2026-07): react-router names every suite `*-test.ts`, and
ALL 5 of the rule's hits over the 2,186-file third-party corpus were in two of
them — `react-router/packages/react-router-dev/vite/remove-exports-test.ts`
(4 hits) and `route-chunks-test.ts` (1). Each is a source-transform fixture
(`"export const keptExport_1 = () => {};…"`) deliberately repeated so the
before/after assertions stay comparable — the exact category the test
exemption exists for. Python's `_paths.is_test_path` has always accepted
`*_test.py`; this closes the same gap on the TS side.


## `GENERATED_MARKER_RE` — the banner half (2026-07-31)

A BANNER match is safe and universal in a way a PATH match is not: no
hand-written file claims to be generator output, so widening the banner costs no
recall, while widening `GENERATED_FILE_RE` silently un-scopes twenty consumer
rules at once.

`immich/packages/sdk/src/fetch-client.ts:1-6` says

    /**
     * Immich
     * 3.1.0
     * DO NOT MODIFY - This file has been generated using oazapfts.
     */

and matched NOTHING: not `generated (with|by)`, not `do not edit`, and its path
has no `generated/` segment. That one file is **606 unactionable findings** —
493 `jsdoc-restates-signature`, 55 `no-enum`, 52 `no-type-member-comment-wall`
(37% of that rule's entire corpus output), 5 `prefer-non-nullable-collection`,
1 `prefer-discriminated-union`.

The obvious widening — bare `auto-?generated`, bare `generated (using|from)`,
bare `do not (modify|change)` — was **measured over 105,551 `.ts`/`.tsx` files
and rejected**. It matched 175 extra files, and the clear majority were ordinary
hand-written source where the phrase is prose or an identifier:

* `refine/packages/react-router/src/document-title-handler.tsx` — `autoGeneratedTitle: string;`
* `grafana/packages/grafana-runtime/src/services/LocationSrv.ts` — "Do not change this unless you are the Angular router."
* `typescript-eslint/.../no-unnecessary-type-assertion.ts` — "Disallow type assertions that do not change the type"
* `prisma/packages/1-framework/3-tooling/cli/vitest.config.ts` — "do not change to 'threads', it will cause the failure"

Requiring the file itself as the SUBJECT of the claim — "this file is / was /
has been … generated", "auto-generated file", "do not modify this file" —
matches **92** extra files instead. All 92 were read: ~84 are true generator
output (payload's 30 `THIS FILE WAS GENERATED AUTOMATICALLY BY PAYLOAD.`
app-router files, twenty's 35 ASCII-art barrel banners, storybook's 9
eslint-plugin configs, nest's 2 GraphQL schemas, medusa's currency tables), and
8 are GENERATOR SOURCES that carry the banner because they emit it
(`twenty/packages/twenty-shared/scripts/generateBarrels.ts`,
`grafana/scripts/cli/themeTemplates/generatedFileBanner.ts`). Those 8 are the
whole recall cost, and every one already sits under a `scripts/` directory.

Finding-level effect over those 92 files, across the 20 rules that consult
`isGeneratedFile`: **645 → 1**. The single survivor is
`prefer-discriminated-union` at `immich/packages/sdk/src/fetch-client.ts:579` —
that rule does not call `isGeneratedFile` at all, which is a separate gap
recorded here rather than fixed in passing.
