name: tsconfig CI

on:
  push:
    branches: [main]
    paths:
      - 'packages/tsconfig/**'
      - '.github/workflows/tsconfig-ci.yml'
  pull_request:
    paths:
      - 'packages/tsconfig/**'
      - '.github/workflows/tsconfig-ci.yml'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: packages/tsconfig
    steps:
      - name: Harden the runner
        uses: step-security/harden-runner@b09bb98e06d4d774595224525879c09bc6e98c40 # v2.20.1
        with:
          egress-policy: audit
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: '24'
      - name: Validate JSON
        run: |
          node -e "JSON.parse(require('fs').readFileSync('base.json','utf8'))"
          node -e "JSON.parse(require('fs').readFileSync('strict.json','utf8'))"
      - name: Smoke test — extend strict.json against a sample
        run: |
          npm install --no-audit --no-fund --save-dev typescript@latest
          mkdir -p smoke/src
          cat > smoke/tsconfig.json <<'EOF'
          {
            "extends": "../strict.json",
            "include": ["src/**/*"],
            "compilerOptions": {
              "noEmit": true,
              "outDir": "out"
            }
          }
          EOF
          cat > smoke/src/index.ts <<'EOF'
          export function add(a: number, b: number): number {
            return a + b;
          }
          EOF
          cd smoke
          ../node_modules/.bin/tsc --noEmit -p tsconfig.json
      - run: npm pack --dry-run
